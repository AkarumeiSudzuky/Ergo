<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Ergo" parent="Base.Theme.Ergo">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@color/app_background_col</item>
        <item name="android:windowLightStatusBar">@color/app_background_col</item>
    </style>
</resources>